- name: variable
  hosts: local
  connection: local
  # vars: # Play Level Variables
  #   GREET: "PLAY"
   vars_files:
    - course.yaml
  # vars_prompt:
  # - name: GREET
  #   prompt: Enter the value
  #   private: false
  tasks:
  - name: printing the variable
    # vars:
    #   GREET: "TASK"
    ansible.builtin.debug:
      msg: "Hello from {{ GREET }}"



#1. Command line or orgs
#2. Task Level
#3. Files
#4. prompt
#5. play
